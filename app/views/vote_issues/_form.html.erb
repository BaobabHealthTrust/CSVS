<%= form_for(@vote_issue) do |f| %>
  <% if @vote_issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_issue.errors.count, "error") %> prohibited this vote_issue from being saved:</h2>

      <ul>
      <% @vote_issue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vote_id %><br />
    <%= f.text_field :vote_id %>
  </div>
  <div class="field">
    <%= f.label :issue_id %><br />
    <%= f.text_field :issue_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
